// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
// };

// export default nextConfig;
module.exports = {
  images: {
    remotePatterns: [new URL("https://reqres.in/img/faces/1-image.jpg")],
  },
}